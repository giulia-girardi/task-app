<div class="app">
    <%- include('./partials/navbar.ejs') %>
    <div class="pagecontent">
        <div class="tabtitle">
            <h2>Tasks list</h2>
            <a href="/tasks/create" id="createtaskbutton">Create a task</a>
        </div>
        <div class="tabpage">
            <h4>Your tasks</h4>
            <% allDueTasks.forEach(task => { %>
                <div class="task">
                    <div class="task-edit-delete">
                        <div class="task-complete">
                            <p class="task-name"><%= task.taskName  %></p>
                        </div>
                        <div class="edit-delete">
                            <form action="/tasks/<%= task.id  %>/done" method="POST">
                            <button class="complete-btn" type="submit" name="taskCompleted"></button>
                            </form>
                            <a class="edit" href='<%= `/tasks/${task.id}/edit` %> '></a>
                            <form action="/tasks/<%= task.id  %>/delete" method="POST">
                            <button class="delete-btn"></button>
                            </form>
                        </div>
                    </div>  
                    <p id="texttasks">Due date: <%= task.dueDate.toDateString()  %> </p>
                    <p id="texttasks">Collaborators: <%= task.collaborators  %> </p>
                </div>
            <% }) %>

            <h4>Tasks shared with you</h4>
            <% sharedTasksDue.forEach(task => { %>
                <div class="task">
                    <div class="task-edit-delete">
                        <div class="task-complete">
                            <p class="task-name"><%= task.taskName  %></p>
                        </div>
                        <div class="edit-delete">
                            <form action="/tasks/<%= task.id  %>/done" method="POST">
                            <button class="complete-btn" type="submit" name="taskCompleted"></button>
                            </form>
                            <a class="edit" href='<%= `/tasks/${task.id}/edit` %> '></a>
                            <form action="/tasks/<%= task.id  %>/delete" method="POST">
                            <button class="delete-btn"></button>
                            </form>
                        </div>
                    </div>  
                    <p id="texttasks">Due date: <%= task.dueDate.toDateString()  %> </p>
                    <p id="texttasks">Collaborators: <%= task.collaborators  %> </p>
                </div>
            <% }) %>
        </div>
    </div>
</div>